# LAN production API (nginx → Django). Trailing slash optional; stripped at runtime.
# Subnet for docs only: 192.168.1.0/24 — this value must be a full URL, not CIDR.
VITE_API_URL=http://192.168.1.4:8092
