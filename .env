# Browser must reach this host (not 127.0.0.1 on live/LAN — that is each user's own PC).
VITE_API_URL=http://192.168.1.4:8092

# Optional: override base images for `docker compose build` (see docker-compose.yml).
# DOCKER_NODE_IMAGE=public.ecr.aws/docker/library/node:18-alpine
# DOCKER_NGINX_IMAGE=public.ecr.aws/docker/library/nginx:alpine